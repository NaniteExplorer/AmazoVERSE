{
  "name": "amaezoverse",
  "version": "1.0.0",
  "private": true,
  "description": "Amaezoverse — full-stack e-commerce monorepo (React client + Express/MongoDB server)",
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm --prefix server run dev\" \"npm --prefix client start\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client start",
    "start": "npm --prefix server start",
    "build": "npm --prefix client run build",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
